import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.naren.examprep',
  appName: 'Exam Prep Tracker',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
